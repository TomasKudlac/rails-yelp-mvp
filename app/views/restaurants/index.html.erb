<div class="container">
  <div class="restaurant-list">
    <h1>Restaurant List</h1>

    <% @restaurants.each do |restaurant| %>
      <h2> <%= link_to restaurant.name, restaurant_path(restaurant) %> </h2>
    <% end %>
    <%= link_to 'Add a new Restaurant', new_path, class: "btn-new-restaurant" %>
  </div>

</div>
